<!-- app/views/resumes/new.html.erb -->

<div class="container mx-auto pt-16">
  <div class="flex justify-center">
    <div class="w-full max-w-md">
      <h1 class="text-center text-2xl font-bold mb-6">New Resume</h1>

      <%= render "form", resume: @resume %>

      <div class="mt-6 text-center">
        <%= link_to "Back to resumes", resumes_path, class: 'bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded inline-block' %>
      </div>
    </div>
  </div>
</div>
