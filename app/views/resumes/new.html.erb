<!-- app/views/resumes/new.html.erb -->

<h1 class="text-2xl font-bold mb-4">New Resume</h1>

<%= render "form", { :resume => @resume } %>

<br>

<div class="mt-4">
  <%= link_to "Back to resumes", resumes_path, { :class => 'text-blue-600 hover:text-blue-800 underline' } %>
</div>
